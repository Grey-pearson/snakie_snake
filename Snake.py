import pygame
import Food

STARTING_POSITIONS = [(0, 20, 0, 20)]
MOVE_DISTANCE = 20


class Snake:
    def __init__(self):

        # attributes for important things about snakei
        self.length = 0
        # may not be needed
        self.alive = True
        self.create_snake()

    def create_snake(self):
        # for posistion in STARTING_POSITIONS:

        # draw head on screen i think? maybe in game loop or
        # just drawing head one time, then make a new def for adding segments
        #

        pass
